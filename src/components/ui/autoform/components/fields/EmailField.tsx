import { AutoFormFieldProps } from "@autoform/react";
import React from "react";

const EmailField: React.FC<AutoFormFieldProps> = ({
  field,
  label,
  id,
  inputProps,
}) => {
  return <div>EmailField</div>;
};

export default EmailField;
