import { AutoFormFieldProps } from "@autoform/react";
import React from "react";

const URLField: React.FC<AutoFormFieldProps> = ({
  field,
  label,
  id,
  inputProps,
}) => {
  return <div>URLField</div>;
};

export default URLField;
