{
  "name": "@apps/backend",
  "version": "0.0.0",
  "main": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot --env-file ../../.env run src/index.ts --console-depth 5",
    "build":"tsc && tsc-alias -p tsconfig.json && bun build dist/index.js --target node --outfile .output/server.js --production --external @packages/db --external @packages/rpc --external pg --external nodemailer --external @sendgrid/mail --minify",
    "start": "bun run dist/server.js",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint src",
    "lint:fix": "biome lint --write src"
  },
  "dependencies": {
    "@packages/db": "workspace:*",
    "@packages/rpc": "workspace:*",
    "better-auth": "^1.3.27",
    "dotenv": "^17.2.3",
    "hono": "^4.9.12",
    "nodemailer": "^7.0.9",
    "@sendgrid/mail": "^8.1.6"
  },
  "devDependencies": {
    "@types/nodemailer": "^7.0.2",
    "typescript": "^5.9.3"
  }
}
