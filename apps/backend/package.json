{
  "name": "@apps/backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --bun --hot run src/index.ts --console-depth 5",
    "build": "bun build src/index.ts --target node --outfile .output/server.js --production",
    "start": "bun run .output/server.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "@packages/db": "workspace:*",
    "@packages/rpc": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
